name: sidebar_menu_and_dashboard_flutter_sdk_by_techieblossom
description: "This is a concept of sidebar menu and dashboard and various states between them when the menu comes in focus and vice versa."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
